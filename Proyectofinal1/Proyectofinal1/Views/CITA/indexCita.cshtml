@model IEnumerable<Proyectofinal1.Models.CITA>

@{
    ViewBag.Title = "indexCita";
    Layout = "~/Views/Shared/_LayoutUsuario.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Fecha_cita)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Observaciones)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.MASCOTA.Nombre_mascota)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.TIPO_CITA.Tipo_cita1)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Fecha_cita)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Observaciones)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.MASCOTA.Nombre_mascota)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.TIPO_CITA.Tipo_cita1)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ID_cita }) |
            @Html.ActionLink("Details", "Details", new { id=item.ID_cita }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ID_cita })
        </td>
    </tr>
}

</table>

<div>
    @Html.ActionLink("Regresar", "indexMascota", "MASCOTA", new { id = Session["ID_usuario"] }, null)
</div>
